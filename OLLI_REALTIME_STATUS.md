# OLLI Realtime 단계별 전환

기준일: 2026-09-11

## 1단계 — 관찰노트

사용자가 PC·Phone 동작 확인 완료를 보고했다. 이번 시간표 작업에서는 관찰노트 저장 코어, Phone 관찰노트 adapter, CAS, 세션 복구, 2초 polling을 변경하지 않았다. 이 기록은 사용자 확인이며 이번 작업의 실기기 재시험을 뜻하지 않는다.

## 2단계 — 시간표와 Phone 오늘 수업 목록

구현 및 로컬 테스트 완료. 운영 배포와 실제 학원에서의 시간표 검증은 별도 단계다.

- 공통 원본: PC `olli-realtime-common.js` 1.1.0. Phone은 기존 rewrite로 이 한 파일을 읽는다.
- 공통 `watchDomain('schedule', callback)`: 같은 학원의 변경 신호를 120ms 단위로 합치고 처리 중 도착한 신호를 보관한다. callback이 `true`를 반환해야 완료다. 편집/조회 중 `false`는 1초 후 재시도, 예외는 5초 후 재시도한다. 신호가 없으면 이 helper는 서버를 주기 조회하지 않는다.
- SUBSCRIBED, online, focus, visibility 복귀 시 변경 번호를 다시 확인한다. 숨김/오프라인에서는 새 조회를 시작하지 않는다.
- 학원·세션 및 제공되는 AcademyContext token이 바뀐 응답은 폐기한다. 이벤트의 revision이나 내용 자체를 화면 데이터로 쓰지 않는다.
- PC `pc-timetable.js`: 기존 revision 조회 → 학생 최신 조회 → `loadWeek` → 화면 반영 경로를 연결한다. 저장·대화상자·입력·기존 조회 중에는 보류한다. 주간 조회 실패 시 revision을 완료로 기록하지 않는다.
- PC `pc-timetable-service.js`: 조회 전후 학원·세션을 확인해 이전 학원 결과를 새 학원의 주간 캐시에 쓰지 않는다.
- Phone `olli-attendance-phone-adapter.js`: 현재 열려 있는 초등/유치부 목록의 오늘 정규수업·보강 배치 정보를 다시 읽는다. 검색/편집/선택 중에는 화면 반영을 보류한다. 오늘 수업 캐시는 학원·세션·날짜·요청별로 구분한다.
- PC 기존 3초 polling을 유지한다. Phone에는 별도 3초 서버 polling을 추가하지 않았다.

### 현재 범위와 다음 단계

`schedule` revision은 배치·이동 외에 출석·대기·보강·픽업·반·담임·메모 등의 변경도 공유한다. 따라서 해당 변경이 신호를 발생시키면 현재 열려 있는 PC 주간 시간표와 Phone 오늘 수업 목록도 다시 읽을 수 있다. 기능별로 독립된 신호라고 설명하지 않는다.

이번 watcher는 PC 출석부 탭에는 연결하지 않는다. Phone 출석 표시 저장/월별 출석 조회, 학생정보 편집 화면의 자동 갱신, 학생 기본정보 전체, 담임·픽업의 별도 화면은 전환 완료로 보지 않는다. 저장 RPC와 SQL/권한/트리거는 이번 작업에서 변경하지 않았다. 기존 시간표 조회가 수행하는 예약변경 적용 등의 동작은 유지된다.

### 원본 배포 순서와 복구

1. PC 공통 helper와 PC adapter를 먼저 main에 반영한다.
2. Phone adapter와 HTML 버전 변경을 반영한다. 공통 파일은 Phone에 복사하지 않는다.
3. 양쪽 배포 결과를 확인한 뒤 아래 실기기 검증을 진행한다.

새 helper는 기존 API에 추가되며 기존 관찰노트 구독은 그대로다. Phone에 이전 공통 파일이 로드되어 `watchDomain`이 없으면 기존 화면 진입 시 동기화가 계속 동작한다. 롤백은 PC·Phone의 이번 변경 커밋을 각각 revert한다. 이 단계에서는 polling을 제거하지 않는다.

## 검증 기록

- Node 24에서 PC/common 15개, Phone 8개 동작 테스트 통과.
- PC: burst 합치기, 다른 학원/관찰노트 신호 무시, 편집 중 보류, 조회 중 후속 신호, 실패 재시도, 숨김/오프라인 복귀, 재구독, 학원/세션 전환, 첫 revision, 주간 조회 실패 후 같은 revision 재시도, 편집 시작 중 응답 폐기, 출석부 범위 제외, 학원 간 캐시 오염 방지.
- Phone: 최신 목록/중복 revision, 편집·선택·숨김 보류, 실패 후 기존 목록 보존, 동기 예외, 학원 전환·요청 순서 역전, 편집 시작 중 렌더 보류, 기존 조회 함수 호환.
- 로컬 브라우저 가상 서버에서 실제 common/PC/Phone adapter를 로드해 PC 4시→5시, Phone 5시→6시 양방향 화면 반영을 확인했다. Phone 미저장 메모 편집 중 PC 변경은 보류되고 편집 종료 후 5시로 갱신됐다. 브라우저 오류 로그 없음.
- 가상 서버는 실제 Supabase WebSocket을 대체했다. 운영 DB 신호 전달, 배포본 캐시, iOS Safari 실기기, 실제 계정 권한은 이 테스트로 검증됐다고 주장하지 않는다.

각 저장소에서 실행: `node --test tests/schedule-realtime.test.cjs`

## 배포 후 실제 PC·Phone 확인

- PC에서 기존 학생의 수업 배치·이동 → Phone 오늘 목록에서 새 시간/목록 이탈 확인.
- Phone 수업시간 저장 → PC 해당 주간 시간표에서 이동 확인.
- 두 기기 연속 변경, 저장 중 외부 변경, 연결 복귀 시 최종 서버 상태 확인.
- 편집 중 입력 유지, 학원 전환 후 이전 학원 학생·수업 미표시 확인.
- 공유 revision 영향: 기존 출석·보강·대기·픽업·반·담임·메모와 관찰노트 동작 회귀 확인.
- 확인 완료 후에만 다음 영역 전환 또는 polling 축소를 결정한다.
